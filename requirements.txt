# Targets Raspberry Pi OS Bookworm (Python 3.11) and newer.
# Bookworm ships prebuilt wheels for armhf/arm64; no need to cap versions
# for ancient Buster-era pip resolution.

psutil>=7.0,<8.0

fastapi>=0.115,<1.0
pydantic>=2.0,<3.0
anyio>=4.0,<5.0

# Plain uvicorn + websockets avoids uvloop/httptools C extensions that can
# be slow to build on older arm boards.  On Bookworm arm64 wheels are fine.
uvicorn>=0.30,<1.0
websockets>=13,<17

# For Form() parsing on /login.
python-multipart>=0.0.9,<1.0
